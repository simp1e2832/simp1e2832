/*
for (int i = 0; i < 2500; i++) {
	tem_sum += *stand_roi.data;
	stand_roi.data++;
}
cout << "tem_sum is " << tem_sum << endl;
for (int i = 0; i < 2500; i++) {
	cout << (int)*stand_roi.data << ",";
	if ((i + 1) % 100 == 0) {
		cout << endl;
	}
	stand_roi.data++;
}

//cross-correlation
for (int i = 0; i < x; i++) {
	for (int j = 0; j < y; j++) {

	}
}
cout << "image data type is " << raw_image.type() << endl;
cout << "image row is " << raw_image.rows << endl;
cout << "image column is " << raw_image.cols << endl;
return 0;
*/